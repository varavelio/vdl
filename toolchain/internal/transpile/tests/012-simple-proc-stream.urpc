version 1

proc GetUser {
  input {
    userId: string
  }

  output {
    user: {
      id: string
      name: string
      email: string
    }
    success: bool
  }
}

stream GetUser {
  input {
    userId: string
  }

  output {
    user: {
      id: string
      name: string
      email: string
    }
    success: bool
  }
}
