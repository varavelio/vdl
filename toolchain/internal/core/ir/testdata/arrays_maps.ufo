type WithArrays {
    tags: string[]
    scores: int[]
    matrix: float[][]
}

type WithMaps {
    metadata: map<string>
    counts: map<int>
    nested: map<map<bool>>
}

type Combined {
    items: map<string[]>
    lookup: map<int>[]
}
