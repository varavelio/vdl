""" Maximum items per page. """
@config
const maxPageSize = 100

""" API version string. """
const apiVersion = "1.0.0"

""" Default tax rate. """
const defaultTaxRate = 0.21

""" Feature flag for new UI. """
const featureNewUi = true

""" Legacy mode disabled. """
const legacyMode = false

const defaultLimit = maxPageSize

const baseConfig = {
  host "localhost"
  port 8080
}

const prodConfig = {
  ...baseConfig
  secure true
}
