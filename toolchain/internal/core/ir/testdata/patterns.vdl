""" Subject template for user events. """
@topicTemplate
const userEventSubject = "events.users.{userId}.{eventType}"

""" Cache key template for sessions. """
@cacheTemplate
const sessionCacheKey = "cache:session:{sessionId}"

""" Simple key template. """
const simpleKey = "key:{id}"
