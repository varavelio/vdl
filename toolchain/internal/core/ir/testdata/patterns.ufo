""" NATS subject for user events. """
pattern UserEventSubject = "events.users.{userId}.{eventType}"

""" Redis cache key for sessions. """
pattern SessionCacheKey = "cache:session:{sessionId}"

""" Simple pattern with one placeholder. """
pattern SimpleKey = "key:{id}"
