version: 1
schema: ./schema.vdl
targets:
  - playground:
      output: ./gen/playground
      defaultBaseUrl: https://api.example.com
      defaultHeaders:
        - key: Authorization
          value: Bearer token
