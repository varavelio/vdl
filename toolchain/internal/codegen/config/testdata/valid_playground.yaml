version: 1
schema: ./schema.vdl
targets:
  - target: playground
    output: ./gen/playground
    options:
      default_base_url: https://api.example.com
      default_headers:
        - key: Authorization
          value: Bearer token
