pattern UserEventSubject = "events.users.{userId}.{eventType}"
pattern SessionCacheKey = "cache:session:{sessionId}"
pattern TopicChannel = "channels.{channelId}.messages.{messageId}"
pattern SimpleKey = "static-key"
pattern DuplicatedSegment = "events.users.{userId}.{eventType}.{userId}"

rpc Service {
  proc Echo {
    input {}

    output {}
  }
}
