// Test schema for features verification (constants and patterns)

const MAX_PAGE_SIZE = 100
const MIN_PAGE_SIZE = 10
const API_VERSION = "2.0.0"
const PI_APPROX = 3.14159
const DEBUG_MODE = false
const ENABLE_CACHE = true

pattern UserEventSubject = "events.users.{userId}.{eventType}"
pattern CacheKey = "cache:{namespace}:{key}"
pattern ApiEndpoint = "/api/v{version}/{resource}/{id}"
pattern DuplicatedSegment = "users.{userId}.{action}.{userId}"

type SimpleType {
  name: string
}
